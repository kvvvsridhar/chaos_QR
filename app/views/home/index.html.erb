<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - QR Code App</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin: 20px;
        }
        h1 {
            margin-bottom: 30px;
        }
        .button-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
        }
        .nav-button {
            padding: 15px 30px;
            font-size: 18px;
            color: white;
            background-color: #007BFF;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
        }
        .nav-button:hover {
            background-color: #0056b3;
        }
        .scan-buttons {
            display: flex;
            flex-direction: row;
            justify-content: center;
            gap: 20px;
        }
        .scan-button {
            padding: 15px 20px;
            font-size: 16px;
            color: white;
            background-color: #28a745;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
        }
        .scan-button:hover {
            background-color: #218838;
        }
    </style>
</head>
<body>
    <h1>Welcome to the QR Code App</h1>
    <div class="button-container">
        <a href="/users/new" class="nav-button">Create New User</a>
        <a href="/users" class="nav-button">View User List</a>
        <a href="<%= qr_code_generator_users_path %>" class="nav-button">Generate QR Codes</a>
        <div class="scan-buttons">
            <a href="/users/scan?scan_type=entry" class="scan-button">Entry Scan</a>
            <a href="/users/scan?scan_type=exit" class="scan-button">Exit Scan</a>
        </div>
    </div>
</body>
</html>
